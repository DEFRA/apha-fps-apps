namespace Apha.Common.Constants
{
    /// <summary>
    /// Represents well-known system user identifiers used across FPS, PACT, and Costbook modules.
    /// </summary>
    public enum SuperUser
    {
        /// <summary>
        /// The default system/fallback user ID used when no authenticated user can be resolved.
        /// </summary>
        SuperUserId = 42
    }
}
