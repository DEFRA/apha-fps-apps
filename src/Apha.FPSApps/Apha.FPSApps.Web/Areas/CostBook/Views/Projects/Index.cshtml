@using Apha.FPSApps.Web.Models.Components.DataGrid
@model Apha.FPSApps.Web.Areas.CostBook.Models.ProjectViewModel

@{
        ViewData["Title"] = "Select project";
}

<style>
    .input-group-text {
        border-radius: 0 5px 5px 0;
        padding: 7px 6px 6px 6px;
        border: 1px solid #919191;
    }
</style>

@* <div class="govuk-grid-row sup_border-bottom">
    <div class="govuk-grid-column-one-half">
        <h1 class="govuk-heading-s sup_margin_0" style="padding:10px 0 10px 10px">Choose Existing Project</h1>
    </div>
    <div class="govuk-grid-column-one-half">
        <div style="float: right; text-align:right; margin-top: 2px; width:50%;">
            <a href="~/costbook/costbookproject_newproject.html" class="govuk-button sup_margin_0">+ Add New Project</a>
        </div>
    </div>
</div> *@

<div class="row">
    <div class="col-lg-6">
        <div style="margin: 2px 0 0 0;">
        </div>
    </div>
    <div class="col-lg-6">
    </div>

    <div class="col-lg-12">
        


            @* <div class="container-fluid" style="padding: 0;"> *@
        <div class="govuk-grid-row sup_margin_left_right_0">
            <div class="govuk-grid-column-full">
                    <div class="align-items-start m-10" id="search-grid">
                        

                        <!-- Grid Container -->
                        <div id="@($"gridContainer_{Model.ProjectGrid.GridId}")">
                            @await Html.PartialAsync("_DataGrid", Model.ProjectGrid)
                        </div>

                   
                </div>
            </div>
            </div>
       
    </div>
</div>

@section Scripts {
    <script>
        window.addEventListener('load', function () {
            const myModal = new bootstrap.Modal(document.getElementById('exampleModal'));
            myModal.show();
        });

        function searchTable() {
            var input, filter, found, table, tr, td, i, j;
            input = document.getElementById("searchInput");
            filter = input.value.toUpperCase();
            table = document.getElementById("tableBody");
            tr = table.getElementsByTagName("tr");
            for (i = 0; i < tr.length; i++) {
                td = tr[i].getElementsByTagName("td");
                for (j = 0; j < td.length; j++) {
                    if (td[j].innerHTML.toUpperCase().indexOf(filter) > -1) {
                        found = true;
                    }
                }
                if (found) {
                    tr[i].style.display = "";
                    found = false;
                } else {
                    tr[i].style.display = "none";
                }
            }
        }
        function viewProject(btn) {
            var parentProject = $(btn).data('id');
            debugger;
            window.location.href = '/Costbook/Projects/Edit/' + encodeURIComponent(parentProject);
        }
        function goToPage(pageNumber) {
            if (pageNumber < 1 || pageNumber > @Model.TotalPages) {
                return;
            }
            window.location.href = '?page=' + pageNumber + '&year=' + @Model.SelectedYear + '&recordsPerPage=' + @Model.RecordsPerPage + '&searchTerm=' + encodeURIComponent('@Model.SearchTerm');
        }

        function closeEditModal() {
            document.getElementById('editModal').style.display = 'none';
        }
    </script>
}