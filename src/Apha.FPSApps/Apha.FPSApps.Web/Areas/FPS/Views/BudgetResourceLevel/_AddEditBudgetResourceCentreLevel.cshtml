@model Apha.FPSApps.Web.Areas.FPS.Models.BudgetResourceCentreLevelItem
@{
    var isEditMode = !string.IsNullOrEmpty(Model.WorkgroupName) && !string.IsNullOrEmpty(Model.Account);
    var title      = isEditMode ? "Update Budget Bid" : "Add Budget Bid";
    var formId     = isEditMode ? "formEditBudgetBid" : "formAddBudgetBid";
    var saveLabel  = isEditMode ? "Update" : "Save";
    var saveAction = isEditMode ? "updateBudgetBid()" : "saveBudgetBid()";
}

<div class="modal-header flex-space-center">
    <h3 class="modal-title" style="margin: 0;" id="budgetBidModalLabel">@title</h3>
    <button type="button" class="btn-close btn-red" onclick="closeModal()">X</button>
</div>
<div class="modal-body" style="overflow-y: auto; max-height: calc(80vh - 120px);">
    <div class="govuk-error-summary" role="alert"
         aria-labelledby="error-summary-title" tabindex="-1" style="display:none;">
        <h2 class="govuk-error-summary__title" id="error-summary-title">
            There is a problem
        </h2>
        <div class="govuk-error-summary__body">
            <ul class="govuk-list govuk-error-summary__list" id="modalErrorList"></ul>
        </div>
    </div>
    <form class="row" id="@formId">
        <input type="hidden" asp-for="WorkgroupName" id="WorkgroupName" />
        <div class="col-12">
            <div class="govuk-form-group sup_margin_0">
                <label for="WorkgroupName" class="govuk-label p-0">Work Group</label>
                <input type="text" asp-for="WorkgroupName" class="govuk-input sup_width_100" readonly>
            </div>
        </div>
        <div class="col-12">
            <div class="govuk-form-group sup_margin_0">
                <label for="Account" class="govuk-label p-0">Account <span class="sup_color_red">*</span></label>
                @if (isEditMode)
                {
                    <input type="text" class="govuk-input sup_width_100" asp-for="@Model.Account" readonly />
                }
                else
                {
                    <select id="Account" name="Account" required class="govuk-select sup_width_100">
                        <option value="">-- Select Account --</option>
                        @if (Model.AccountList != null)
                        {
                            foreach (var account in Model.AccountList)
                            {
                                <option value="@account.Value">@account.Text</option>
                            }
                        }
                    </select>
                    <span asp-validation-for="Account" class="govuk-error-message" style="display:none;"></span>
                }
            </div>
        </div>
        <div class="col-12">
            <div class="govuk-form-group sup_margin_0">
                <label for="GenBid" class="govuk-label" asp-for="GenBid">Gen Bid <span class="sup_color_red">*</span></label>
                <div class="govuk-input__wrapper">
                    <div class="govuk-input__prefix" aria-hidden="true">£</div>
                    <input type="number" step="0.01" asp-for="GenBid" class="govuk-input" id="GenBid">
                </div>
                <span asp-validation-for="GenBid" class="govuk-error-message" style="display:none;"></span>
            </div>
        </div>
    </form>
</div>
<div class="modal-footer sup_flex_space_center">
    <button class="govuk-button govuk-button--secondary sup_margin_0" data-module="govuk-button" onclick="closeModal()">Cancel</button>
    <button class="govuk-button sup_margin_0" data-module="govuk-button" type="button" onclick="@saveAction">@saveLabel</button>
</div>
