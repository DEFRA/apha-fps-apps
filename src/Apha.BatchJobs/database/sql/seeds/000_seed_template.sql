-- Seed template.
-- Keep this file business-neutral. Add concrete seeds in new files like:
-- 001_seed_lookup_values.sql
-- 002_seed_demo_jobs.sql

BEGIN;

-- Example placeholder (disabled intentionally):
-- INSERT INTO fps.some_lookup_table(code, name)
-- VALUES ('EXAMPLE', 'Example')
-- ON CONFLICT DO NOTHING;

COMMIT;
