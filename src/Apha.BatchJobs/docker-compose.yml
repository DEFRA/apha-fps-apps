services:
  postgres:
    image: postgres:16-alpine
    container_name: batch_jobs_postgres
    environment:
      POSTGRES_DB: batch_jobs_foundation_db
      POSTGRES_USER: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - batch_jobs_network

  batch-jobs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: batch_jobs_app
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__BatchJobsConnectionString: Host=postgres;Port=5432;Database=batch_jobs_foundation_db;Username=postgres;Password=<REDACTED>;Timeout=30
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - batch_jobs_network

volumes:
  postgres_data:
    driver: local

networks:
  batch_jobs_network:
    driver: bridge
